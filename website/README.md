# Website Handoff

Files:
- `index.html`
- `styles.css`

To finish the site:
1. Replace the `video-placeholder` block in `index.html` with your demo `<video>` or embed.
2. Replace the two placeholder download button `href="#"` values in the downloads section.
3. Deploy the `website/` folder as a static site.
